server:
  port: 8072


eureka:
  client:
    fetch-registry: true   # allows the Gateway to know other services
    register-with-eureka: true  # allows Gateway to register itself
    service-url:
      defaultZone: http://localhost:8070/eureka/
